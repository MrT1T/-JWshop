# 🛍️ Online Store API  

Простое REST API для онлайн-магазина, написанное на **Node.js**, **Express.js** и **TypeScript**.  
Данные хранятся в JSON-файлах (без базы данных), что делает проект лёгким для старта и изучения.  

---

## 🚀 Возможности
- Управление товарами (**CRUD API**)  
- Управление пользователями (создание и получение списка)  
- Создание заказов (с проверкой наличия товара и обновлением stock)  
- Валидация данных через [Zod](https://zod.dev)  
- Поддержка `.env` для конфигурации  

---

## 📂 Структура проекта
jwServer/
├─ package.json
├─ tsconfig.json
├─ .env.example
└─ src/
├─ index.ts
├─ app.ts
├─ routes/
├─ controllers/
├─ services/
├─ models/
├─ middlewares/
└─ utils/

## 🔧 Установка и запуск

Установить зависимости
npm install

Настроить окружение
Скопировать .env.example → .env

cp .env.example .env

Запуск в режиме разработки
npm run dev

Сборка и запуск
npm run build
npm start

## 🌐 API endpoints

Products
GET    /api/products        # список товаров
GET    /api/products/:id    # товар по id
POST   /api/products        # создать товар
PATCH  /api/products/:id    # обновить товар
DELETE /api/products/:id    # удалить товар

Users
GET  /api/users        # список пользователей
POST /api/users        # создать пользователя

Orders
GET  /api/orders        # список заказов
POST /api/orders        # создать заказ

## 🛠 Используемые технологии

Node.js
Express.js
TypeScript
Zod— валидация входных данных
UUID — генерация ID
Morgan — логирование запросов
CORS